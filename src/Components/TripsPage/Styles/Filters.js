import { styled } from 'styletron-react';

export const FilterBlock = styled('div', {
    display: 'flex',
    display: 'column',
    color: 'black',
    height: '100%',
    overflowY: 'scroll',
});


export const FilterNameLeft = styled('div', {
    display: 'flex',
    display: 'column',
    color: 'black',
    height: '100%',
    overflowY: 'scroll',
});

export const FilterNameRight = styled('div', {
    display: 'flex',
    display: 'column',
    color: 'black',
    height: '100%',
    overflowY: 'scroll',
});